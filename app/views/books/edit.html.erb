<div class="container">
  <h1>Editing: <%= @book.title %></h1>
    <div class="col-md-9">
      <%= render 'edit_book', book: @book %>
  </div>

  <div class="col-md-3 pull-right">
    <%= link_to 'Back To Book', @book, class: 'btn btn-success' %><br>
    <%= link_to 'Books Index', books_path, class: 'btn btn-info' %>
  </div>
</div>
